import RPi.GPIO as IO
import time

IO.setwarnings(False)
IO.setmode(IO.BOARD)

#testPin = 12 # temp
#IO.setup(testPin, IO.OUT) # temp 

class test:
	def __init__(self, pwm = 0, drxn = ""):
		self.pwm = pwm; # set pwm as zero
		self.drxn = drxn # empty character as direction

	if drxn == 'w' or drxn == 'W':
		drxn = "Forward" # if user enters this key change the string in "direction" to "Forward"
	if drxn == 's' or drxn == 'S':
		drxn = "Reverse"
	if drxn == 'd' or drxn == 'D':
		drxn = "Right Turn"
	if drxn == 'a' or drxn == 'A':
		drxn = "Left Turn"
	if pwm >= 0:
		pwm = int(pwm)
	def desc(self):
		descStr = "PWM is %f, direction is %s" % (self.pwm, self.drxn)
		return descStr # return string with pwm and direction

while True:
	userIn = raw_input()
	Test = test()
	if userIn != 'q' or userIn != 'Q':
		test.drxn = userIn
		print(Test.desc())
		continue
	if type(userIn) == int:
		num == int(userIn)
		test.pwm = num
		test.drxn = userIn
		print(Test.desc())
		continue
	if userIn == 'q' or userIn == 'Q':
		break


